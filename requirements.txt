# AcademiCare — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core Data Science ──────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# ── Machine Learning (ML Models 1, 3, 4, 5) ───
scikit-learn>=1.3.0

# ── Deep Learning (ML Model 2 — LSTM) ─────────
tensorflow>=2.13.0          # For LSTM training
# OR: tensorflow-cpu>=2.13.0  # Lighter version

# ── Model Serialization ────────────────────────
joblib>=1.3.0

# ── Backend API ────────────────────────────────
fastapi>=0.103.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
python-jose[cryptography]>=3.3.0  # JWT auth
passlib[bcrypt]>=1.7.4

# ── Database ───────────────────────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.7    # PostgreSQL driver
alembic>=1.12.0           # DB migrations

# ── Firebase (push notifications) ─────────────
firebase-admin>=6.2.0

# ── Visualization ──────────────────────────────
matplotlib>=3.7.0
plotly>=5.17.0

# ── Utilities ─────────────────────────────────
python-dotenv>=1.0.0
httpx>=0.25.0
